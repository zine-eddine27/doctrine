{% extends 'base.html.twig' %}

{% block title %}Blog-{% endblock %}

{% block body %}



{% for type , messages in app.flashes %}
    <div class="alert alert-{{type}}" >
        {% for message in messages %}
            {{ message }}
        {% endfor %}        
    </div>
{% endfor %}

<form action="{{ path('login') }}" method="post">
    <div class="row">
        
         <div class="col-md-4 mt-5">
            <p class="h5"><u>Connexion à votre compte</u></p>
            <p>Pas de compte ??<a href="{{ path('subscribe') }}"> <u>S'inscrire ici</u></a></p>
            
            <div class="form-group">
                <label for="">Email</label>
                <input type="text" label="email" class="form-control" placeholder="Email.." name="_username">                
            </div>
            <div class="form-group">
                <label for="">Mot de passe</label>
                <input type="password" class="form-control" placeholder="Votre mot de passe.." name="_password">
            </div>
            <button type="submit" class="btn btn-success">Se connecter !</button>
        </div>
    </div>
</form>
    
{% endblock %}
